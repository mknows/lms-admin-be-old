const { User } = require("../../../models");

module.exports = {
  /**
   * @desc      Get Enrolled Subject (Semua data Materi)
   * @route     GET /api/v1/my-study/my-enrolled-subject
   * @access    Private\\
   */
  enrolledSubject: async (req, res) => {

  }
}